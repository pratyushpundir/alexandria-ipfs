# =============================================================================
# Alexandria IPFS Service - Environment Configuration
# =============================================================================
# Copy this file to .env for local development outside Docker.
# When running via docker-compose, these are set in the root .env file.
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
GRPC_PORT=9092

# -----------------------------------------------------------------------------
# Blockfrost IPFS Configuration
# -----------------------------------------------------------------------------
# Get your project ID from https://blockfrost.io/
# Create an IPFS project (free tier available)
BLOCKFROST_IPFS_PROJECT_ID=your_blockfrost_ipfs_project_id
BLOCKFROST_IPFS_BASE_URL=https://ipfs.blockfrost.io/api/v0

# Gateway URL for retrieving pinned content
IPFS_GATEWAY_URL=https://ipfs.blockfrost.dev/ipfs
